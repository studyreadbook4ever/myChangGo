{
  "name": "260729-ttt-museum",
  "version": "1.0.0",
  "description": "중학생도 한 바퀴 체험하며 배우는 AI 계량경제학·칸토로비치 웹 박물관",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "check": "node --check src/model.js && node --check src/app.js && node --check scripts/serve.mjs && node --check scripts/browser-smoke.mjs",
    "test": "node --test",
    "test:browser": "node scripts/browser-smoke.mjs",
    "verify": "npm run check && npm test && npm run test:browser"
  }
}
