{
  "name": "@relayplay/example-live-race",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.worker.json --noEmit && tsc -p tsconfig.node.json && vite build",
    "dev": "vite --host 127.0.0.1",
    "dev:node": "tsx node/index.ts",
    "dev:worker": "wrangler dev --config wrangler.jsonc --port 8787",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@relayplay/client": "0.1.0",
    "@relayplay/cloudflare": "0.1.0",
    "@relayplay/core": "0.1.0",
    "@relayplay/node": "0.1.0",
    "@relayplay/server": "0.1.0"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  },
  "engines": {
    "node": ">=24.15.0"
  }
}
